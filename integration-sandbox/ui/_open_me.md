# Open the sandbox website

Start from:

```text
integration-sandbox/ui/home.html
```

`index.html` from the first draft is kept as a simple prototype page. The full case-based website starts at `home.html`.
